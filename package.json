{
  "name": "@pocket-tui/workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "check": "cargo check --workspace && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "build:ts": "tsc -p packages/core/tsconfig.json && tsc -p packages/pocketjs/tsconfig.json",
    "build:native": "cargo build -p pocket-tui-napi --release && node scripts/link-native.mjs",
    "build": "cargo build -p pocket-tui-napi --release && node scripts/link-native.mjs && bun run build:ts",
    "test": "cargo test --workspace && bun test packages/core/src/protocol.test.ts packages/pocketjs/test examples/todo-list/src/todo-model.test.ts examples/todo-list/src/backend-smoke.test.ts examples/rule-shift/src/engine.test.ts examples/rule-shift/src/presentation.test.ts examples/rule-shift/src/backend-smoke.test.ts && bun run --cwd packages/pocketjs contract:upstream"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.2"
  }
}
