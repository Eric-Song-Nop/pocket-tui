{
  "name": "@pocket-tui/workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "check": "cargo check --workspace && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "build:ts": "tsc -p packages/core/tsconfig.json",
    "build:native": "cargo build -p pocket-tui-napi --release && node scripts/link-native.mjs",
    "build": "cargo build -p pocket-tui-napi --release && node scripts/link-native.mjs && tsc -p packages/core/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.2"
  }
}
